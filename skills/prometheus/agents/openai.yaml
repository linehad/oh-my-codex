interface:
 display_name: "Prometheus - Plan Builder"
 short_description: "Plan-only builder with assumptions, gates, research, and verification."
 default_prompt: "Use Prometheus in plan-only mode: ask for Plan Mode when needed, use required named agents for non-trivial planning, review with Momus, and wait for approval before implementation."
